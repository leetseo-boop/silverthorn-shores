## Goal
Give you a downloadable copy of the homepage hero image that briefly shows before the video loads.

## What it is
`src/assets/home-hero-marina.webp` — the marina photo used as the hero background in `SilverthornHomePage.tsx` (line 401) while the YouTube embed loads. Thorn appears in it.

## Steps
1. Copy the file to the project's Files area as `home-hero-marina.webp` so you can preview and download it.
2. Also export a PNG version alongside it, in case you want a format that's easier to open/edit outside a browser.
3. No changes to the website itself.
