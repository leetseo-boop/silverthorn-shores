## Refine nav & footer styling

1. **Nav** (`SilverthornHomePage.tsx` ~145): change bg from `#1B2B3A` → white; nav link/text color from white → navy (`#1B2B3A`); logo `h-10` → `h-14`, drop white background wrapper (logo sits on white now).
2. **Footer** (`SilverthornHomePage.tsx` ~710): change bg `#0D1E2B` → white; all footer text colors → navy / muted navy; logo `h-12` → `h-20`, drop white background wrapper.
3. Keep orange "Book Now" CTA as accent for contrast on white.
4. Mobile menu panel: also switch to white bg / navy text.