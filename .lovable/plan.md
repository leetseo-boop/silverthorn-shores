Remove the "Map preview" badge from `src/components/ResortMap.tsx` so only the new branded location block remains on `/directions` and `/contact`.

Change:
- Delete the absolute-positioned badge at the top-left of the map panel (lines 60–65).
- Keep the gradient background, location card, and "Open in Google Maps" button intact.

No other files or pages need changes.