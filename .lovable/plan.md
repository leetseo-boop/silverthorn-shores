## Swap resort map image

1. Copy `user-uploads://official_silverthorn_cartoon_map_shasta_lake-3.png` → `src/assets/silverthorn-resort-map.png` (overwrite). Keeps the existing import path so no other code changes are needed.
2. Update `src/routes/cabins.tsx` line 346 `alt` to: `Silverthorn Resort map 2026 Shasta Lake`.

That's it — no other files affected.