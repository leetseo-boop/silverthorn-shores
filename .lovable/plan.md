Remove the `ferry` gallery entry (lines 36-41) from `gallery` array in `src/components/HistoryPage.tsx`. The `ferry` import stays — it's still used in the sticky figure on the right of the top story section.